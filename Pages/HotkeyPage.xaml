<Page x:Class="SelectUnknown.Pages.HotkeyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SelectUnknown.Pages" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Background="#FFFFDFED"
      Loaded="Page_Loaded"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HotkeyPage">

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden">
        <StackPanel>
            <!-- 启动快捷键 -->
            <Border Padding="5" Background="#FFFBE8F0" CornerRadius="5" Margin="8,8,8,0" ToolTip="框定即搜快捷键">
                <DockPanel>
                    <TextBlock Text="启动快捷键" VerticalAlignment="Center" FontSize="14" />
                    <TextBox Name="StartKeyInput" Width="200" VerticalAlignment="Center" 
                             Margin="10,0,0,0" 
                             PreviewKeyDown="StartKeyInput_PreviewKeyDown" 
                             IsReadOnly="True" />
                    <Button Content="设置启动热键" Margin="10,0,0,0" VerticalAlignment="Center" 
                            Click="SetStartHotKeyButton_Click"/>
                </DockPanel>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Direction="270" Opacity="0.5" Color="Black"/>
                </Border.Effect>
            </Border>

            <!-- 截图快捷键 -->
            <Border Padding="5" Background="#FFFBE8F0" CornerRadius="5" Margin="8,8,8,0">
                <DockPanel>
                    <TextBlock Text="截图快捷键" VerticalAlignment="Center" FontSize="14" />
                    <TextBox Name="ScreenshotKeyInput" Width="200" VerticalAlignment="Center" 
                             Margin="10,0,0,0" 
                             PreviewKeyDown="ScreenshotKeyInput_PreviewKeyDown" 
                             IsReadOnly="True" />
                    <Button Content="设置截图热键" Margin="10,0,0,0" VerticalAlignment="Center" 
                            Click="SetScreenshotHotKeyButton_Click"/>
                </DockPanel>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Direction="270" Opacity="0.5" Color="Black"/>
                </Border.Effect>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
